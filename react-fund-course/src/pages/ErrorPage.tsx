import React from 'react'

type ErrorPageProps = {}

const ErrorPage = (props: ErrorPageProps) => {
  return (
    <h1 style={{color: 'red'}}>
        Page not exists!
    </h1>
  )
}

export default ErrorPage